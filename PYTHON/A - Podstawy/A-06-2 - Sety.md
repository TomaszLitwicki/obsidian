`my_first_set = {'Tori','Figo','Wrangler'}`
`empty_set = {}` - to NIE JEST PUSTY SET! To zrobił się SŁOWNIK
`really_empty_set = set()`
W matematyce SET to ZBIÓR
Set nie jest uporządkowany kolejnością
Nie pozwala na duplikaty

Operacje na SETACH:

| kod                               | działanie                                   |
| --------------------------------- | ------------------------------------------- |
| `my_set.add('element')`           | dodaje 1 element do setu                    |
| `my_set.update({el1, el2`         | dodaje wiele elementów *el1,el2...* do setu |
| `my_set.remove('el  en   )`       | usuwa *element* z setu                      |
| `set1.union(set2)`                | \| (pipe) - łączenie                        |
| `set1.intersection(set2)`         | & (and) - część wspólna                     |
| `set1.difference(set2)`           | - (minus) - różnica                         |
| `set1.symmetric_difference(set2)` | ^ - różnica symetryczna                     |
