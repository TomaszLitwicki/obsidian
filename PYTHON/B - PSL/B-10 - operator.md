`import operator

wprowadza operatory matematyczne na liczbach L1 i L2:
operator.add(L1,L2) + dodawanie 
operator.sub(L1,L2) - odejmowanie
operator.mul(L1,L2) * mnożenie
operator.truediv(L1,L2) / dzielenie

